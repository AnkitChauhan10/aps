pipeline {
    agent any

    stages {
      

        stage('Build Docker Image') {
            steps {
                // Build the Docker image
                sh 'docker build -t aaa:lts'
            }
        }

       
    }
}
